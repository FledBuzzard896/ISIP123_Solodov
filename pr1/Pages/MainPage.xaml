<Page x:Class="pr1.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="account" HorizontalAlignment="Right" Margin="5"/>
        <TextBlock Text="Магазинчик" FontSize="20" HorizontalAlignment="Center" Margin="5" Grid.Row="1"/>
        <ListBox x:Name="products_lb" Margin="5" Grid.Row="2" Background="LightGray" ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock Text="{Binding ProductName}" FontSize="16" Margin="2"/>
                        <TextBlock Text="{Binding Subcategories_.SubcategoryName, StringFormat={}Тип: {0}}" FontSize="12" Margin="2"/>
                        <TextBlock Text="{Binding Manufacturers_.ManufacturerName, StringFormat={}Производитель: {0}}" FontSize="12" Margin="2"/>
                        <TextBlock Text="{Binding CurrentPrice, StringFormat={}{0} ₽}" FontSize="14" Margin="2"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
