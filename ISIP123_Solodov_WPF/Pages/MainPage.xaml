<Page x:Class="ISIP123_Solodov_WPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISIP123_Solodov_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="400"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="23*"/>
        </Grid.RowDefinitions>
        <TextBlock TextAlignment="Center" VerticalAlignment="Center" Text="Пиццерия 5 ночей с Гордовым!" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Margin="10,10,0,0" Text="Выберите пиццу:"/>
            <ListBox Margin="10" x:Name="PizzaListBox" Height="100">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding name, StringFormat=Название: {0}}"/>
                            <TextBlock Text="{Binding description, StringFormat=Описание: {0}}"/>
                            <TextBlock Text="{Binding price, StringFormat=Цена: {0}}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        
    </Grid>
</Page>
