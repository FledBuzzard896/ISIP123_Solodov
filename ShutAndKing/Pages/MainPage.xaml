<Page x:Class="ShutAndKing.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShutAndKing.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        
        <Button x:Name="ExitBtn" Grid.Column="0" Content="Выйти" Margin="10"/>
        <Button x:Name="AccountBtn" Grid.Column="2" Margin="10">
            <Image Source="/Images/squidward.png"/>
        </Button>

        <ScrollViewer VerticalScrollBarVisibility="Hidden" Grid.Row="1" Grid.Column="2" Margin="10">
            <StackPanel Orientation="Vertical">
                <Button Height="35" Width="35" Margin="0,5,0,5"/>
                <Button Height="35" Width="35" Margin="0,5,0,5"/>
                <Button Height="35" Width="35" Margin="0,5,0,5"/>
                <Button Height="35" Width="35" Margin="0,5,0,5"/>
                <Button Height="35" Width="35" Margin="0,5,0,5"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
