<Window x:Class="ShutAndKing.Dialogs.ChangePassword_Dialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShutAndKing.Dialogs"
        mc:Ignorable="d"
        Title="ChangePassword_Dialog" Height="400" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="CloseBtn" 
		Content="Закрыть" FontSize="14"
		Width="75" Height="20"
		HorizontalAlignment="Left"
		Margin="5"
		Grid.Row="0"
		Click="CloseBtn_Click"/>
        <TextBlock Text="СМЕНА ПАРОЛЯ" FontSize="20" HorizontalAlignment="Center" Margin="5" Grid.Row="0"/>
        <TextBlock Text="Введите новый пароль:" FontSize="14" Margin="5" Grid.Row="1"/>
        <TextBox x:Name="firstPassword" HorizontalAlignment="Stretch" FontSize="14" MaxLength="50" Margin="5" Grid.Row="2"/>
        <TextBlock Text="Повторите пароль:" FontSize="14" Margin="5" Grid.Row="3"/>
        <TextBox x:Name="secondPassword" HorizontalAlignment="Stretch" FontSize="14" MaxLength="50" Margin="5" Grid.Row="4"/>
        <Button x:Name="ChangeBtn"
		    Content="Выполнить" FontSize="14"
		    Width="100" Height="20" 
		    Margin="5"
            Grid.Row="5"
		    VerticalAlignment="Bottom"
		    Click="ChangeBtn_Click"/>
    </Grid>
</Window>
